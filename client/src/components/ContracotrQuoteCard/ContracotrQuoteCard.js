import React from "react";
import { useContractorContext } from "../../utils/ContractorContext";
import "./style.css";

const CustomerCard = (props) => {
  const [ContractorState, dispatch] = useContractorContext();
  
  return (
  );
};

export default CustomerCard;
